general:
  artifacts:
    - "build/libs/*.jar"
machine:
  java:
    version: oraclejdk8
  services:
    - docker
dependencies:
  override:
      - mvn dependency:resolve
      - mv build/libs/* $CIRCLE_ARTIFACTS
      - docker build -t vlanderson/sloos-test .
test:
    override:
      - mvn verify
