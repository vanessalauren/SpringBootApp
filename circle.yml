general:
machine:
  java:
    version: oraclejdk8
  services:
    - docker
dependencies:
  override:
      - mvn dependency:resolve
      - mv ../target/* $CIRCLE_ARTIFACTS
      - docker build -t vlanderson/sloos-test .
test:
    override:
      - mvn verify
