general:
artifacts:
    - "~/target"/* # anything in target directory
machine:
  java:
    version: oraclejdk8
  services:
    - docker
dependencies:
  override:
      - mvn dependency:resolve
      - docker build -t vlanderson/sloos-test .
test:
    override:
      - mvn verify   
deployment:
    dev:
    commands:
        - mv "../target"/* "{$CIRCLE_ARTIFACTS}"
      